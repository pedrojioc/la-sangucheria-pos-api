// Re-exported from shared-kernel so both orders and establishment contexts
// can reference the same canonical TaxType enum without a cross-context import.
export { TaxType } from '@shared/domain/value-objects/tax-type'

