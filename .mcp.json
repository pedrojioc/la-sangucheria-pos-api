{
  "mcpServers": {
    "postgres": {
      "command": "uvx",
      "args": ["--with", "mcp<2", "postgres-mcp", "--access-mode=restricted"],
      "env": {
        "DATABASE_URI": "${MCP_POSTGRES_URI}"
      }
    }
  }
}
